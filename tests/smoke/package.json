{
  "name": "bunite-smoke-test",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "echo 'smoke test disabled — pending schema-driven rewrite'"
  },
  "dependencies": {
    "bunite-core": "workspace:*"
  }
}
