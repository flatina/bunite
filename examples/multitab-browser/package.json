{
  "name": "multitab-browser",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx vite build && bun src/main.ts",
    "build": "bunite-build && bunx vite build",
    "typecheck": "echo 'multitab-browser typecheck skipped (legacy RPC pending schema-driven migration)'"
  },
  "dependencies": {
    "bunite-core": "workspace:*",
    "bunite-native-win-x64": "workspace:*"
  },
  "devDependencies": {
    "bunite-dev": "workspace:*",
    "vite": "^6"
  }
}
