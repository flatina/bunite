{
  "name": "bunite-workspace",
  "description": "Uniting UI and Bun",
  "private": true,
  "type": "module",
  "workspaces": [
    "package",
    "tools/*",
    "examples/*",
    "tests/*"
  ],
  "scripts": {
    "typecheck": "bun x tsc -b --noEmit && bun run --filter './examples/*' typecheck",
    "check": "bun run typecheck",
    "build:native:win": "cmake -S package -B package/build/win -DBUNITE_TARGET_ARCH=x64 && cmake --build package/build/win --config Release"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "ts-morph": "^28.0.0",
    "typescript": "^6.0.2"
  }
}
